<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mybatis.configuration.example.mapper.UserMapper">

    <select id="selectAll" resultType="org.mybatis.configuration.example.entity.User">
        SELECT * FROM t_user;
    </select>

</mapper>